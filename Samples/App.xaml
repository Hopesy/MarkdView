<Application x:Class="Samples.App" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Samples" StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{DynamicResource Markdown.Toolbar.Button.Background}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Markdown.Toolbar.Button.Border}" />
            <Setter Property="Foreground" Value="{DynamicResource Markdown.Toolbar.Foreground}" />
            <Setter Property="BorderThickness" Value="1" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Markdown.Toolbar.Button.HoverBackground}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource Markdown.Toolbar.Button.HoverBorder}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Markdown.Toolbar.Button.PressedBackground}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
