<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0-windows</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <UseWPF>true</UseWPF>
    <!-- NuGet 包信息 -->
    <PackageId>MarkdView</PackageId>
    <Version>1.0.10</Version>
    <Authors>Hopesy</Authors>
    <Company>MinoChat</Company>
    <Product>MarkdView</Product>
    <Description>Modern WPF Markdown renderer with streaming support, syntax highlighting, emoji rendering, and Mac-style code blocks. 基于 Markdig 的现代化 WPF Markdown 渲染库，支持流式渲染、代码高亮、Emoji 彩色渲染和 Mac 风格代码块。</Description>
    <PackageTags>wpf;markdown;markdig;streaming;syntax-highlighting;ai;emoji;code-block</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <RepositoryUrl>https://github.com/hopesy/MarkdView</RepositoryUrl>
    <PackageProjectUrl>https://github.com/hopesy/MarkdView</PackageProjectUrl>
    <RepositoryType>git</RepositoryType>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <PackageReleaseNotes>v1.0.10: 调整渲染页面内部留白；优化代码块复制按钮为纯图标样式；修正标题栏高度与无背景图标交互；持续改进深浅主题视觉一致性。</PackageReleaseNotes>
    <PackageIcon>icon.png</PackageIcon>
  </PropertyGroup>
  <!-- 包含外层README 和 LICENSE 到 NuGet 包 -->
  <ItemGroup>
    <None Include="..\README.md" Pack="true" PackagePath="\" />
    <None Include="..\LICENSE" Pack="true" PackagePath="\" />
    <None Include="icon.png" Pack="true" PackagePath="\" />
  </ItemGroup>
  <!--依赖其他包-->
  <ItemGroup>
    <PackageReference Include="Emoji.Wpf" Version="0.3.4" />
    <PackageReference Include="Markdig" Version="0.43.0" />
  </ItemGroup>
  <ItemGroup>
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
      <_Parameter1>MarkdView.Tests</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>
  <!-- 主题资源字典 -->
  <ItemGroup>
    <Resource Include="Themes\MarkdView.Dark.xaml" />
    <Resource Include="Themes\MarkdView.Light.xaml" />
  </ItemGroup>
</Project>
